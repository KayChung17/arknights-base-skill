# 2.0.0 发布说明

2.0.0 聚焦基建技能叠加正确性、可直接使用的排班文件和发布门禁。

## 排班产物

完整项目运行现在生成 `schedule.json`。该文件兼容 `skills/ark-base-schedule/assets/template.json`，包含班次、贸易站、制造站、发电站、控制中枢、无人机节点和产品配置。`result.json` 继续承担完整计算追溯，`report.md` 用于阅读。

`schedule.json` 会写入 `summary.json` 和 `run-manifest.json`，`arkbase verify` 同时检查文件存在、哈希和模板结构。

## 操作与无人机约束

- 每日上线次数与上线时刻分离；`objective.online_schedule.mode=optimize` 会生成候选时间网格供候选评估流程比较，显式 `online_times` 仍可锁定固定时刻。
- 宿舍等级属于布局搜索变量，宿舍床位与重复日心情闭环同步验证。
- 重复日排班默认禁止无人机浪费；无人机优先用于合成玉订单，龙门币不足时可用于赤金或龙门币生产，并要求库存流闭环。

## 技能机制

- 生产相关“同种效果取最高”按 `effect_key` 聚合。
- 同技能槽升级阶段按 `variant_group` 选择最高已解锁版本。
- “与部分技能有特殊叠加规则”使用显式交互规则和优先级。
- 控制中枢技能按实际进驻位置生效。
- 八幡海铃 E2“家族认可”按贸易站内叙拉古干员数量计算。
- 未被无人机加速的经验站，在保持合成玉、龙门币、碎片和赤金指标后继续消除免费可提升的低效组合。
- 右侧功能设施等级作为不可逆硬输入显式确认；右满固定耗电 190，缺电布局在预检或外层搜索阶段淘汰。

## 验证

2.0.0 增加数据结构门禁、选中设施技能覆盖检查、排班模板契约、运行产物绑定和免费经验支配检查。候选库发生截断时，结果继续标记为当前搜索设置中的最高分候选。

## 升级

旧项目配置需要补齐 2.0 严格预检要求的目标、基建状态和求解时域。旧输出目录需要补导 `schedule.json`，迁移步骤见 [MIGRATION-2.0.0.md](MIGRATION-2.0.0.md)。

## 数据范围

内置快照包含 248 名干员、506 条技能。它是经过脱敏的结构化社区快照，不含个人拥有状态、练度或原始导出行，也不代表完整游戏数据库。
