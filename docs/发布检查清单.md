# 发布检查清单

## 代码与测试

- [ ] `bash scripts/validate-all.sh` 通过。
- [ ] 新机制包含单元测试和至少一个端到端回归。
- [ ] JSON Schema 可解析。
- [ ] Python 3.10、3.11、3.12、3.13 CI 通过。
- [ ] 打包暂存目录中的发布包内容已再次运行完整校验。
- [ ] 端到端产物包含 `schedule.json`，且通过模板契约校验。
- [ ] `secondary_output_postprocess.checked=true`，且经验站检查范围内没有 `remaining_dominated_empty_slots`。
- [ ] 右侧功能设施等级已显式确认且作为不可逆硬约束；右满回归用例固定耗电 190。
- [ ] 固定排班和布局搜索都会拒绝 `spare_power < 0` 的配置。

## 数据

- [ ] 更新 `assets/data-version.json`。
- [ ] `data-version.json` 与 `operator-skills.json` 的 `data_version` 一致。
- [ ] 导入警告为零，或在发布说明中逐项列出。
- [ ] 数据来源、日期和适用版本明确。
- [ ] 发布包排除 `roster.xlsx`、roster TSV/CSV、本地配置和求解输出。
- [ ] 脱敏样例中没有个人库存、心情、账号或本地路径。

## 结果与措辞

- [ ] 输出包含可复现清单。
- [ ] `audit_result.py` 通过。
- [ ] 未把截断候选库结果写成全局最优。
- [ ] 报告列出龙门币、碎片、赤金和无人机闭环。
- [ ] 培养建议标注不可逆或非单调技能。

## 发布物

- [ ] 更新版本号、CHANGELOG 和迁移说明。
- [ ] 更新当前版本的发布说明与数据来源。
- [ ] 生成 ZIP、TAR.GZ 和 SHA-256。
- [ ] 生成源目录与打包暂存目录两份验证报告。
- [ ] 检查 LICENSE、NOTICE、SECURITY 和贡献指南。
