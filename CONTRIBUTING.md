# 贡献指南

## 开发环境

```bash
python -m pip install "scipy>=1.11" "openpyxl>=3.1"
bash scripts/validate-all.sh
```

## 提交原则

1. 代码、文档和用户输出以中文为主，字段 ID 与 JSON Schema 保持稳定英文标识。
2. 新增机制必须说明单位、作用房间、作用产品、触发条件、叠加规则和来源。
3. 解析器不得静默丢弃无法识别的记录；应写入 warning 或标记为待核验。
4. 不得为未结构化技能猜测数值。
5. 修改代理目标时必须同步检查最终模拟和审计器。
6. 修改布局或无人机模型时必须增加电力、库存和重复日测试。
7. 培养优化必须识别不可逆或非单调技能，并支持状态对照。
8. 不提交个人 roster、库存、心情、求解输出和临时缓存。
9. 不得把截断候选库或时间限制 incumbent 写成实际全局最优。

## Pull Request 必须包含

- 问题说明和影响范围。
- 数据或机制来源。
- 新增或更新的测试。
- 运行 `bash scripts/validate-all.sh` 的结果。
- 对输出 Schema、兼容性和迁移的说明。

## 数据更新

优先使用 `skills/update-ark-skills` 中的导入脚本，保留原始描述和来源行。生成文件不建议手工修改；确需补充机制标签时，应在变更说明中列明理由。
